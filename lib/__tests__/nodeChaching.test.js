const assert = require('assert');
const nodeChaching = require('../index.js');

describe('nodeChaching', () => {
  it('has a test', () => {
    assert(false, 'nodeChaching should have a test');
  });
});
